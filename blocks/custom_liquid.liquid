{%- comment -%} Auto Generated Variables start {%- endcomment -%}
{%- liquid
  assign block_type = "custom_liquid"
  assign custom_liquid = block.settings.custom_liquid
  assign custom_css = block.settings.custom_css
-%}
{%- comment -%} Auto Generated Variables end {%- endcomment -%}

{% capture selector %}[data-style-id="{{ section.id }}--{{ block.id }}"]{% endcapture %}

<style>{{ custom_css | replace: '&', selector | strip_newlines }}</style>

<div data-style-id="{{ section.id }}--{{ block.id }}" {{ block.shopify_attributes }}>
  {{ custom_liquid }}
</div>

{% schema %}
{
  "name": "Custom Liquid Code",
  "settings": [
    {
      "type": "liquid",
      "id": "custom_liquid",
      "label": "Liquid Code"
    },
    {
      "type": "paragraph",
      "content": "t:all.empty_char"
    },
    {
      "type": "paragraph",
      "content": "t:all.empty_char"
    },
    {
      "type": "header",
      "content": "Block Globals"
    },
    {
      "type": "textarea",
      "id": "custom_css",
      "label": "Custom CSS",
      "default": "& {\n}\n"
    }
  ],
  "presets": [
    {
      "name": "Custom Liquid Code",
      "category": "Custom"
    }
  ],
  "tag": null,
  "class": "shopify_block__custom_liquid"
}
{% endschema %}
