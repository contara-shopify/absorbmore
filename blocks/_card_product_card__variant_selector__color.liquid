{%- comment -%} Auto Generated Variables start {%- endcomment -%}
{%- liquid
  assign block_type = "_card_product_card__variant_selector__color"
  assign section_type = "card_product_card"
  assign legend_label = block.settings.legend_label
  assign legend = block.settings.legend
  assign legend_class = block.settings.legend_class
  assign primary_source = block.settings.primary_source
  assign fallback_source = block.settings.fallback_source
  assign show_tooltip = block.settings.show_tooltip
  assign select_on_hover = block.settings.select_on_hover
  assign content_flow = block.settings.content_flow
  assign truncate_text = block.settings.truncate_text
  assign sold_out_variants = block.settings.sold_out_variants
  assign custom_css = block.settings.custom_css
  assign show_conditionally = block.settings.show_conditionally
-%}
{%- comment -%} Auto Generated Variables end {%- endcomment -%}

{% liquid
  assign content_legend = legend | replace: "[heading]", legend_label
  case content_flow
    when "show_all"
      assign container_classes = 'flex-wrap'
    when "truncate"
      assign container_classes = 'overflow-hidden'
    when "overflow"
      assign container_classes = 'overflow-x-auto'
  endcase
%}

{% if request.design_mode or metaobjects.product_cards[section.settings.product_card] == blank %}
  {% capture selector %}[data-style-id="{{ block.id | split: "__" | last }}"]{% endcapture %}

  <style>
    {{ custom_css | replace: '&', selector | strip_newlines }}
    {% if request.design_mode %}
    {% assign query_selector = '[data-section-id="'|append: section.id | append: '"][data-preview-container] ' | append: selector %}
    {{- custom_css | replace: '&', query_selector  | strip_newlines | replace: '@media screen and', '@container'| replace: '@media', '@container' -}}
    {% endif %}
  </style>
{% endif %}

<div
    data-style-id="{{ block.id | split: "__" | last }}" {{ block.shopify_attributes }}
    x-data="{ hoverValue: '', offset: p_card?.product?.variants?.length, primary_source: `{{ primary_source }}`, fallback_source: `{{ fallback_source }}`}"
    {% if sold_out_variants == 'hide' %}
      x-show="p_card?.product?.variants?.filter(v => v.available)?.length && p_card?.product?.variants?.[0]?.title !== 'Default Title' && ($editor.select_block_id === `{{ block.id }}` || $data.showConditionally('{{ show_conditionally }}', $data?.bundle))"
    {% else %}
      x-show="p_card?.product?.variants?.length && p_card?.product?.variants?.[0]?.title !== 'Default Title' && ($editor.select_block_id === `{{ block.id }}` || $data.showConditionally('{{ show_conditionally }}', $data?.bundle))"
    {% endif %} x-cloak
    class="overflow-hidden"
    x-ignore>
  <legend :id="`legend-{{ block.id }}--${p_card?.product_handle}`"
          class="flex w-full max-w-full truncate {{ legend_class }}"
          data-legend="{% render '_input.inline_richtext', content: content_legend, escape: true %}"
          x-effect="$el.innerHTML = $el.getAttribute('data-legend').replace('[value]', hoverValue || p_card?.selected_variant?.title)">
  </legend>
  <fieldset :aria-labelledby="`legend-{{ block.id }}--${p_card?.product_handle}`"
            class="relative w-full flex scrollbar-none {{ container_classes }}">
    <template
        {% if sold_out_variants == 'hide' %}
          x-for="variant in p_card?.product?.variants?.filter(v => v.available).slice(0, offset)"
        {% else %}
          x-for="variant in p_card?.product?.variants.slice(0, offset)"
        {% endif %}
    >
      <label data-loop-item
             class="flex shrink-0 cursor-pointer border-gray-300 relative !outline-none border-none ac:border-solid"
          {% if sold_out_variants == 'hide' %}
            x-show="variant?.available"
          {% endif %}
             x-effect="$el.classList.toggle('active', p_card?.selected_variant?.id === variant?.id)"
          {% if select_on_hover and show_tooltip %}
            @mouseenter="$tooltip.addTooltip($el, variant?.title); $data.setSelectedVariant(variant?.id)"
            @mouseleave="$tooltip.removeTooltip($el)"
          {% elsif select_on_hover %}
            @mouseenter="$data.setSelectedVariant(variant?.id)"
          {% elsif show_tooltip %}
            @mouseenter="$tooltip.addTooltip($el, variant?.title)"
            @mouseleave="$tooltip.removeTooltip($el)"
          {% endif %}
             @pointerenter="hoverValue = variant?.title"
             @pointerleave="hoverValue = ''"
             :aria-label="variant?.title"
             :style="getVariantSwatches(primary_source, fallback_source, variant)">
        <input type="radio"
               class="sr-only"
               :name="`${p_card?.random_id}-${p_card?.product?.id}-variant_selector__color`"
            {% if sold_out_variants == 'disable' %}
              :disabled="!variant?.available"
            {% endif %}
            {% if show_tooltip %}
              @focus="$tooltip.addTooltip($el?.parentElement, variant?.title)"
              @blur="$tooltip.removeTooltip($el.parentElement)"
            {% endif %}
               @change="$data.setSelectedVariant(variant?.id)"
               :checked="p_card?.selected_variant?.id === variant?.id"
               :value="variant?.id">
      </label>
    </template>
    {% if content_flow == 'truncate' %}
      <div class="flex shrink-0 items-center text-sm font-medium"
           :class="(offset + 1) < p_card?.product?.variants?.length ? '':'opacity-0 pointer-events-none'"
           x-init="$nextTick(async ()=> { if (window.design_mode) { await utils.delay(20) } offset = utils.truncateChildren($el, $el.parentElement)})"
           @resize.window.debounce.200ms="offset = p_card?.product?.variants?.length; await utils.delay(1); offset = utils.truncateChildren($el, $el.parentElement)"
           x-html="utils.applyInlinePluralization(`{{ truncate_text }}`.replace('[count]', `${p_card?.product?.variants?.length - offset}`).replace('[option]', `{{ legend_label }}`))">
        +10 {{ legend_label }}
      </div>
    {% endif %}
  </fieldset>
</div>



{% schema %}
{
  "name": "t:blocks.variant_color_image_swatch.name",
  "settings": [
    {
      "type": "text",
      "id": "legend_label",
      "label": "Heading",
      "default": "Variants"
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "inline_richtext",
      "id": "legend",
      "label": "Heading",
      "default": "[heading]: [value]",
      "info": "[Shortcodes](https://getplatter.atlassian.net/wiki/external/YTBhYmQxMzFhOWQ1NGQ4MzhlNzY1MjZiNDU1YmQ5ODk) Use [heading] as a placeholder for the title, and [value] as a placeholder for the current / hover value."
    },
    {
      "type": "text",
      "id": "legend_class",
      "label": "Heading class",
      "default": "richtext-md h5"
    },
    {
      "type": "paragraph",
      "content": "t:all.empty_char"
    },
    {
      "type": "paragraph",
      "content": "t:all.empty_char"
    },
    {
      "type": "header",
      "content": "Swatches"
    },
    {
      "type": "select",
      "id": "primary_source",
      "label": "Primary source: Variant...",
      "default": "image",
      "options": [
        {
          "value": "title",
          "label": "Title"
        },
        {
          "value": "image",
          "label": "Image"
        },
        {
          "value": "metafield",
          "label": "Metafield"
        }
      ]
    },
    {
      "type": "select",
      "id": "fallback_source",
      "label": "Fallback source:",
      "default": "metafield",
      "options": [
        {
          "value": "title",
          "label": "Title"
        },
        {
          "value": "image",
          "label": "Image"
        },
        {
          "value": "metafield",
          "label": "Metafield"
        }
      ]
    },
    {
      "type": "paragraph",
      "content": "The 'Title' option is setup via the Global Sections for color swatches, this matches the 'title' field of the Metaobject. The setup for the 'Image' is inferred from the selected variant for the option or the product image as a fallback. The 'Metafield' option is inferred from Metaobject data source which can be set to either 'color' or 'image'."
    },
    {
      "type": "checkbox",
      "id": "show_tooltip",
      "label": "Show tooltip on hover",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "select_on_hover",
      "label": "Select on hover"
    },
    {
      "type": "paragraph",
      "content": "t:all.empty_char"
    },
    {
      "type": "paragraph",
      "content": "t:all.empty_char"
    },
    {
      "type": "header",
      "content": "Inline Layout"
    },
    {
      "type": "select",
      "id": "content_flow",
      "label": "Content flow",
      "default": "show_all",
      "options": [
        {
          "value": "show_all",
          "label": "Show all"
        },
        {
          "value": "truncate",
          "label": "Truncate"
        },
        {
          "value": "overflow",
          "label": "Slider"
        }
      ]
    },
    {
      "type": "text",
      "id": "truncate_text",
      "label": "Truncate text",
      "default": "+[count] [option]",
      "info": "Use [count] and [option] as placeholders for values.",
      "visible_if": "{{ block.settings.content_flow == 'truncate' }}"
    },
    {
      "type": "select",
      "id": "sold_out_variants",
      "label": "Sold Out Variants",
      "default": "",
      "options": [
        {
          "value": "",
          "label": "Show"
        },
        {
          "value": "hide",
          "label": "Hide"
        },
        {
          "value": "disable",
          "label": "Disable"
        }
      ]
    },
    {
      "type": "paragraph",
      "content": "t:all.empty_char"
    },
    {
      "type": "paragraph",
      "content": "t:all.empty_char"
    },
    {
      "type": "header",
      "content": "Block Globals"
    },
    {
      "type": "textarea",
      "id": "custom_css",
      "label": "Custom CSS",
      "default": "& {\n\n}\n& legend {\n  margin-bottom: 6px;\n}\n& fieldset {\n  gap: 6px;\n}\n& label {\n  width: 36px;\n  height: 36px;\n  border-width: 1px;\n  border-style: solid;\n  border-radius: 9999px;\n  outline-width: 2px;\n  outline-offset: 2px;\n}\n"
    },
    {
      "type": "select",
      "id": "show_conditionally",
      "label": "Show conditionally",
      "default": "always",
      "options": [
        {
          "value": "always",
          "label": "Always"
        },
        {
          "value": "with_subscriptions",
          "label": "Subscription"
        },
        {
          "value": "no_subscriptions",
          "label": "One-time"
        },
        {
          "value": "no_bundle",
          "label": "Hide in Bundle"
        },
        {
          "value": "bundle",
          "label": "Only in Bundle"
        },
        {
          "value": "with_variants",
          "label": "With Variants"
        },
        {
          "value": "no_variants",
          "label": "No Variants"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Variant: Color / Image Swatch",
      "category": "** Card **"
    }
  ],
  "tag": null,
  "class": "shopify_block___card_product_card__variant_selector__color"
}
{% endschema %}
