{%- comment -%} Auto Generated Variables start {%- endcomment -%}
{%- liquid
  assign block_type = "_card_product_card__collapsible"
  assign section_type = "card_product_card"
  assign label = block.settings.label
  assign accordion_class = block.settings.accordion_class
  assign content = block.settings.content
  assign show = block.settings.show
  assign auto_close_siblings = block.settings.auto_close_siblings
  assign custom_css = block.settings.custom_css
  assign hide_if_empty = block.settings.hide_if_empty
  assign show_conditionally = block.settings.show_conditionally
-%}
{%- comment -%} Auto Generated Variables end {%- endcomment -%}



{% liquid
  if show_conditionally == 'with_subscriptions' and product.selling_plan_groups.size <= 0
    break
  endif
  if show_conditionally == 'no_subscriptions' and product.selling_plan_groups.size > 0
    break
  endif
  if show_conditionally == 'with_variants' and product.variants.size <= 1
    break
  endif
  if show_conditionally == 'no_variants' and product.variants.size > 1
    break
  endif
  if show_conditionally == 'bundle' and is_bundle
    break
  endif
  if show_conditionally == 'no_bundle' and is_bundle != true
    break
  endif
%}

{% capture blocks_content %}{% render "card_product_card._render_blocks",
    product: product,
    variant: variant,
    section: section,
    blocks: block.blocks,
    block_order: block.block_order,
    preorder: preorder,
    preorder_date: preorder_date,
    preorder_quantity: preorder_quantity,
    price: price,
    compare_at_price: compare_at_price,
    is_bundle: is_bundle,
    is_pdp: is_pdp,
    preload_images: preload_images
%}{% endcapture %}

{% liquid
  assign stripped_content = content | strip_html

  assign stripped_blocks_content = blocks_content | strip

  if stripped_content == blank and stripped_blocks_content == blank or label == blank
    assign is_empty = true
  endif
  if is_empty and hide_if_empty != 'none'
    capture hide_conditionally
      if hide_if_empty == 'block'
        echo '<script type="block/hide"></script>'
        assign hide_attribute = 'data-hide-if-empty-block'
      endif
      if hide_if_empty == 'container'
        echo '<script type="container/break"></script>'
        assign hide_attribute = 'data-hide-if-empty-container'
      endif
      if hide_if_empty == 'section'
        echo '<script type="section/break"></script>'
        assign hide_attribute = 'data-hide-if-empty-section'
      endif
    endcapture

    if request.design_mode != true and hide_conditionally contains '<script'
      echo hide_conditionally
      break
    endif
    assign hide_attribute = hide_attribute | append: ' data-block-type="' | append: block_type | append: '"' | append: ' data-block-id="' | append: block_id | append: '"'
  endif
%}

{% capture accordion_content %}{% render '_input.richtext', content: content %}{% endcapture %}
{% if stripped_blocks_content != blank %}
  {% assign accordion_content = blocks_content %}
{% endif %}

{% if accordion_content contains '<script type="container/break"></script>' %}
  {% break %}
{% endif %}

{% capture html_attributes %}
  data-style-id="{{ style_id }}"  {{ hide_attribute }}
  {% if show_conditionally != blank and show_conditionally != 'always' %}
    x-cloak
    x-show="$editor.select_block_id === `{{ block_id }}` || $el.querySelector(`[data-style-id='${$editor?.select_block_id}']`) || $data.showConditionally('{{ show_conditionally }}', $data?.bundle)"
  {% endif %}
{% endcapture %}

{% render '_components.collapsible', label: label, content: accordion_content, default_open: show, class: accordion_class, html_attributes: html_attributes, auto_close_siblings: auto_close_siblings %}
