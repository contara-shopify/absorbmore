{%- comment -%} Auto Generated Variables start {%- endcomment -%}
{%- liquid
  assign section_type = "card_article_card"
  assign _handle = section.settings.handle
  assign article_card = section.settings.article_card
  assign preview_article = section.settings.preview_article
  assign link_to_object = section.settings.link_to_object
  assign dynamic_text_color = section.settings.dynamic_text_color
  assign dynamic_background_color = section.settings.dynamic_background_color
  assign dynamic_border_color = section.settings.dynamic_border_color
  assign container_spacing = section.settings.container_spacing
  assign text_color = section.settings.text_color
  assign background_color = section.settings.background_color
  assign custom_css = section.settings.custom_css
  assign section_id = section.settings.section_id
  assign override_preview_background = section.settings.override_preview_background
-%}
{%- comment -%} Auto Generated Variables end {%- endcomment -%}

{% liquid
  if article_card != blank
    assign _handle = article_card | json | handleize
  endif
  assign card_class = _handle | handleize | prepend: "article-card--"
%}

{% capture render_card %}
  <article
      class="block {{ card_class }} {{ container_spacing }}"
      x-init="utils.barbaPrefetchTargetPage($el, `{{ article.url }}`)"
      x-defer="interact"
      x-ignore
      {% if link_to_object %}
        href="{{ article.url }}"
        {% if open_in_new_tab %}
          target="_blank"
        {% endif %}
      {% endif %}
      {% if dynamic_text_color != blank or dynamic_background_color != blank or dynamic_border_color != blank %}
        style="color: {% render '_input.dynamic_content', content: dynamic_text_color, keys: 'article', article: article %};
            background: {% render '_input.dynamic_content', content: dynamic_background_color, keys: 'article', article: article %};
            border-color: {% render '_input.dynamic_content', content: dynamic_border_color, keys: 'article', article: article %};"
      {% endif %} >
    {% render "card._render_blocks",
        card_object: article,
        section: section,
        blocks: section.blocks,
        block_order: section.block_order,
        preload_images: preload_images
    %}
  </article>
{% endcapture %}

{% if link_to_object %}
  {% assign render_card = render_card | replace: '<article', '<a' | replace: '</article', '</a' %}
{% else %}
  {% assign render_card = render_card | replace: '<article', '<div' | replace: '</article', '</div' %}
{% endif %}

{{ render_card }}
