{%- comment -%} Ingredients Section — "What's Inside" {%- endcomment -%}
{%- liquid
  assign headline = section.settings.headline
  assign headline_color = section.settings.headline_color
  assign headline_size_desktop = section.settings.headline_size_desktop
  assign headline_size_mobile = section.settings.headline_size_mobile
  assign subtext = section.settings.subtext
  assign subtext_color = section.settings.subtext_color
  assign subtext_size_desktop = section.settings.subtext_size_desktop
  assign subtext_size_mobile = section.settings.subtext_size_mobile
  assign legend_1_label = section.settings.legend_1_label
  assign legend_1_color = section.settings.legend_1_color
  assign card_bg = section.settings.card_bg
  assign card_border = section.settings.card_border
  assign card_border_radius = section.settings.card_border_radius
  assign card_bg_hover = section.settings.card_bg_hover
  assign card_border_hover = section.settings.card_border_hover
  assign image_border_radius = section.settings.image_border_radius
  assign stats_color = section.settings.stats_color
  assign background_color = section.settings.background_color
  assign max_width_desktop = section.settings.max_width_desktop
  assign custom_css = section.settings.custom_css
  assign section_id = section.settings.section_id
-%}

{% capture selector %}[data-style-id="{{ section.id }}"]{% endcapture %}

{% capture css %}
  & {
    position: relative;
    width: 100%;
    {% if background_color != blank %}background-color: {{ background_color }};{% endif %}
  }

  & .ig__container {
    max-width: {{ max_width_desktop }}px;
    margin: 0 auto;
    padding: 40px 20px;
  }

  & .ig__header {
    display: flex;
    flex-direction: column;
    gap: 16px;
    margin-bottom: 32px;
  }

  & .ig__headline {
    font-weight: 700;
    line-height: 1.1;
    margin: 0;
    font-size: {{ headline_size_mobile }}px;
    {% if headline_color != blank and headline_color.alpha != 0 %}color: {{ headline_color }};{% endif %}
  }
  & .ig__headline p { margin: 0; }

  & .ig__subtext {
    line-height: 1.6;
    margin: 0;
    font-size: {{ subtext_size_mobile }}px;
    {% if subtext_color != blank and subtext_color.alpha != 0 %}color: {{ subtext_color }};{% endif %}
  }
  & .ig__subtext p { margin: 0; }

  & .ig__legend {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
  }

  & .ig__legend-item {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 6px 16px;
    border-radius: 100px;
    font-size: 15px;
    font-weight: 600;
    border: 1px solid #d1d5db;
    background-color: #ffffff;
    pointer-events: none;
    cursor: default;
  }

  & .ig__legend-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }

  /* ── Grid ── */
  & .ig__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 16px;
  }

  /* ── Card ── */
  & .ig__card {
    display: flex;
    flex-direction: column;
    gap: 0;
    padding: 16px;
    border-radius: {{ card_border_radius }}px;
    {% if card_bg != blank %}background-color: {{ card_bg }};{% endif %}
    {% if card_border != blank %}border: 1px solid {{ card_border }};{% endif %}
    position: relative;
    transition: background-color 0.3s, border-color 0.3s;
  }

  & .ig__card:hover {
    {% if card_bg_hover != blank %}background-color: {{ card_bg_hover }};{% endif %}
    {% if card_border_hover != blank %}border-color: {{ card_border_hover }};{% endif %}
  }

  & .ig__card-dot {
    position: absolute;
    top: -5px;
    left: 50%;
    transform: translateX(-50%);
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }

  & .ig__card-image {
    width: 100%;
    aspect-ratio: 21 / 9;
    object-fit: cover;
    border-radius: {{ image_border_radius }}px;
    margin-bottom: 16px;
  }

  & .ig__card-title {
    font-weight: 700;
    font-size: 16px;
    line-height: 1.3;
    margin: 0 0 4px;
  }

  & .ig__card-subtitle {
    font-size: 13px;
    line-height: 1.4;
    margin: 0 0 10px;
    opacity: 0.55;
  }

  & .ig__card-desc {
    font-size: 14px;
    line-height: 1.55;
    margin: 0;
    flex-grow: 1;
  }
  & .ig__card-desc p { margin: 0; }

  & .ig__card-stats {
    font-size: 12px;
    line-height: 1.5;
    margin-top: 16px;
    font-weight: 500;
    color: {{ stats_color }};
  }

  /* ── Desktop ── */
  @media (min-width: 768px) {
    & .ig__container {
      padding: 80px 48px;
    }
    & .ig__header {
      flex-direction: row;
      justify-content: space-between;
      align-items: flex-start;
      gap: 40px;
      margin-bottom: 40px;
    }
    & .ig__header-left {
      flex-shrink: 0;
    }
    & .ig__header-right {
      display: flex;
      flex-direction: column;
      gap: 16px;
      align-items: flex-start;
      max-width: 560px;
    }
    & .ig__headline {
      font-size: {{ headline_size_desktop }}px;
    }
    & .ig__subtext {
      font-size: {{ subtext_size_desktop }}px;
    }
    & .ig__grid {
      grid-template-columns: repeat(4, 1fr);
      gap: 20px;
    }
    & .ig__card {
      padding: 20px;
    }
    & .ig__card-title {
      font-size: 18px;
    }
    & .ig__card-subtitle {
      font-size: 14px;
    }
    & .ig__card-desc {
      font-size: 14px;
    }
    & .ig__card-dot {
      top: -5px;
      left: 50%;
    }
  }

  @media (min-width: 480px) and (max-width: 767px) {
    & .ig__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
{% endcapture %}

{% capture section_content %}
  <style>
    {{ css | append: custom_css | replace: '&', selector | strip_newlines }}
  </style>

  <section
    data-style-id="{{ section.id }}"
    {% if section_id != blank %}id="{{ section_id }}"{% endif %}
  >
    <div class="ig__container">

      <div class="ig__header">
        <div class="ig__header-left">
          {% if headline != blank %}
            <h2 class="ig__headline">{{ headline }}</h2>
          {% endif %}
        </div>
        <div class="ig__header-right">
          {% if subtext != blank %}
            <div class="ig__subtext">{{ subtext }}</div>
          {% endif %}
          {% if legend_1_label != blank %}
            <div class="ig__legend">
              <span class="ig__legend-item">
                <span class="ig__legend-dot" style="background-color: {{ legend_1_color }};"></span>
                {{ legend_1_label }}
              </span>
            </div>
          {% endif %}
        </div>
      </div>

      <div class="ig__grid">
        {% for block in section.blocks %}
          {%- liquid
            assign card_image = block.settings.image
            assign card_title = block.settings.title
            assign card_subtitle = block.settings.subtitle
            assign card_desc = block.settings.description
            assign card_stats = block.settings.stats
            assign dot_color = block.settings.dot_color
            assign block_card_bg = block.settings.card_bg
            assign block_card_border = block.settings.card_border
            assign block_card_bg_hover = block.settings.card_bg_hover
            assign block_card_border_hover = block.settings.card_border_hover
          -%}

          {% capture block_selector %}[data-style-id="{{ section.id }}--{{ block.id }}"]{% endcapture %}

          <style>
            {% if block_card_bg != blank %}{{ block_selector }}.ig__card { background-color: {{ block_card_bg }} !important; }{% endif %}
            {% if block_card_border != blank %}{{ block_selector }}.ig__card { border-color: {{ block_card_border }} !important; }{% endif %}
            {% if block_card_bg_hover != blank %}{{ block_selector }}.ig__card:hover { background-color: {{ block_card_bg_hover }} !important; }{% endif %}
            {% if block_card_border_hover != blank %}{{ block_selector }}.ig__card:hover { border-color: {{ block_card_border_hover }} !important; }{% endif %}
          </style>

          <div
            class="ig__card"
            data-style-id="{{ section.id }}--{{ block.id }}"
            {{ block.shopify_attributes }}
          >
            {% if dot_color != blank %}
              <span class="ig__card-dot" style="background-color: {{ dot_color }};"></span>
            {% endif %}

            {% if card_image != blank %}
              <img
                class="ig__card-image"
                src="{{ card_image | image_url: width: 600 }}"
                alt="{{ card_image.alt | default: card_title }}"
                width="{{ card_image.width }}"
                height="{{ card_image.height }}"
                loading="lazy"
              >
            {% endif %}

            {% if card_title != blank %}
              <h3 class="ig__card-title">{{ card_title }}</h3>
            {% endif %}

            {% if card_subtitle != blank %}
              <p class="ig__card-subtitle">{{ card_subtitle }}</p>
            {% endif %}

            {% if card_desc != blank %}
              <div class="ig__card-desc">{{ card_desc }}</div>
            {% endif %}

            {% if card_stats != blank %}
              <div class="ig__card-stats">{{ card_stats }}</div>
            {% endif %}
          </div>
        {% endfor %}
      </div>

    </div>
  </section>
{% endcapture %}

{{ section_content }}

{% schema %}
{
  "name": "CTR Ingredients",
  "settings": [
    {
      "type": "header",
      "content": "Headline"
    },
    {
      "type": "richtext",
      "id": "headline",
      "label": "Headline",
      "default": "<p>What's inside.<br>And why it matters.</p>"
    },
    {
      "type": "color",
      "alpha": true,
      "id": "headline_color",
      "label": "Headline color"
    },
    {
      "type": "range",
      "id": "headline_size_desktop",
      "label": "Headline size — Desktop (px)",
      "default": 48,
      "min": 20,
      "max": 100,
      "step": 1,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "headline_size_mobile",
      "label": "Headline size — Mobile (px)",
      "default": 36,
      "min": 16,
      "max": 72,
      "step": 1,
      "unit": "px"
    },
    {
      "type": "paragraph",
      "content": "t:all.empty_char"
    },
    {
      "type": "header",
      "content": "Subtext"
    },
    {
      "type": "richtext",
      "id": "subtext",
      "label": "Subtext",
      "default": "<p>We don't just use the right ingredients, we make sure your body can actually absorb them. Every active compound is clinically studied, precisely dosed, and rigorously third-party tested.</p>"
    },
    {
      "type": "color",
      "alpha": true,
      "id": "subtext_color",
      "label": "Subtext color"
    },
    {
      "type": "range",
      "id": "subtext_size_desktop",
      "label": "Subtext size — Desktop (px)",
      "default": 16,
      "min": 12,
      "max": 28,
      "step": 1,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "subtext_size_mobile",
      "label": "Subtext size — Mobile (px)",
      "default": 15,
      "min": 12,
      "max": 24,
      "step": 1,
      "unit": "px"
    },
    {
      "type": "paragraph",
      "content": "t:all.empty_char"
    },
    {
      "type": "header",
      "content": "Legend"
    },
    {
      "type": "text",
      "id": "legend_1_label",
      "label": "Legend label",
      "default": "WLP-1™ — 2 capsules daily"
    },
    {
      "type": "color",
      "id": "legend_1_color",
      "label": "Legend dot color",
      "default": "#3b82f6"
    },
    {
      "type": "paragraph",
      "content": "t:all.empty_char"
    },
    {
      "type": "header",
      "content": "Cards Global"
    },
    {
      "type": "color",
      "id": "card_bg",
      "label": "Card background",
      "default": "#e8eef6"
    },
    {
      "type": "color",
      "id": "card_border",
      "label": "Card border",
      "default": "#b8cce4"
    },
    {
      "type": "range",
      "id": "card_border_radius",
      "label": "Card border radius (px)",
      "default": 12,
      "min": 0,
      "max": 32,
      "step": 2,
      "unit": "px"
    },
    {
      "type": "color",
      "id": "card_bg_hover",
      "label": "Card background — hover"
    },
    {
      "type": "color",
      "id": "card_border_hover",
      "label": "Card border — hover"
    },
    {
      "type": "range",
      "id": "image_border_radius",
      "label": "Image border radius (px)",
      "default": 10,
      "min": 0,
      "max": 24,
      "step": 2,
      "unit": "px"
    },
    {
      "type": "color",
      "id": "stats_color",
      "label": "Stats text color",
      "default": "#3b82f6"
    },
    {
      "type": "paragraph",
      "content": "t:all.empty_char"
    },
    {
      "type": "header",
      "content": "Section Globals"
    },
    {
      "type": "range",
      "id": "max_width_desktop",
      "label": "Max width desktop (px)",
      "min": 800,
      "max": 1600,
      "step": 20,
      "default": 1440,
      "unit": "px"
    },
    {
      "type": "color",
      "alpha": true,
      "id": "background_color",
      "label": "Section background",
      "default": "#f7f5f1"
    },
    {
      "type": "textarea",
      "id": "custom_css",
      "label": "Custom CSS"
    },
    {
      "type": "text",
      "id": "section_id",
      "label": "Section Id"
    }
  ],
  "blocks": [
    {
      "type": "ingredient",
      "name": "Ingredient Card",
      "limit": 8,
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Name + dosage",
          "default": "IGOB131® - 300mg"
        },
        {
          "type": "text",
          "id": "subtitle",
          "label": "Subtitle",
          "default": "African Mango extract"
        },
        {
          "type": "richtext",
          "id": "description",
          "label": "Description",
          "default": "<p>Rebalances metabolism hormones for appetite control and fat loss. Targets leptin and adiponectin directly.</p>"
        },
        {
          "type": "text",
          "id": "stats",
          "label": "Stats line",
          "default": "28.1 lbs lost · 6.7\" off waist · 48.6% leptin reduction · 159% adiponectin increase"
        },
        {
          "type": "color",
          "id": "dot_color",
          "label": "Dot color",
          "default": "#3b82f6",
          "info": "Indicator dot at the top of the card."
        },
        {
          "type": "paragraph",
          "content": "t:all.empty_char"
        },
        {
          "type": "header",
          "content": "Card Color Override",
          "info": "Leave blank to use global card colors."
        },
        {
          "type": "color",
          "id": "card_bg",
          "label": "Background override"
        },
        {
          "type": "color",
          "id": "card_border",
          "label": "Border override"
        },
        {
          "type": "color",
          "id": "card_bg_hover",
          "label": "Background hover override"
        },
        {
          "type": "color",
          "id": "card_border_hover",
          "label": "Border hover override"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "CTR Ingredients",
      "category": "Content",
      "blocks": [
        {
          "type": "ingredient",
          "settings": {
            "title": "IGOB131® - 300mg",
            "subtitle": "African Mango extract",
            "description": "<p>Rebalances metabolism hormones for appetite control and fat loss. Targets leptin and adiponectin directly.</p>",
            "stats": "28.1 lbs lost · 6.7\" off waist · 48.6% leptin reduction · 159% adiponectin increase",
            "dot_color": "#3b82f6"
          }
        },
        {
          "type": "ingredient",
          "settings": {
            "title": "CQR-300® - 300mg",
            "subtitle": "Cissus quadrangularis extract",
            "description": "<p>Natural fat and carb blocker — inhibits lipase and amylase. Head-to-head with oral semaglutide showed comparable weight loss over 16 weeks.</p>",
            "stats": "321% GLP-1 boost · 17.8% DPP-4 reduction",
            "dot_color": "#3b82f6"
          }
        },
        {
          "type": "ingredient",
          "settings": {
            "title": "Capsifen® - 200mg",
            "subtitle": "Bioavailable capsaicin",
            "description": "<p>Promotes thermogenesis without GI discomfort. Reduces appetite and cravings. 20x more absorbed than standard capsaicin.</p>",
            "stats": "4 lbs lost in 4 weeks · no behavior change required",
            "dot_color": "#3b82f6"
          }
        },
        {
          "type": "ingredient",
          "settings": {
            "title": "Body Ready™ Tripeptide",
            "subtitle": "Nanoemulsion peptide complex",
            "description": "<p>Targets appetite suppression through the endocannabinoid system. Includes Grains of Paradise, Humulene nanoemulsion, and reduced Glutathione.</p>",
            "stats": "28.1 lbs lost · 6.7\" off waist · 48.6% leptin reduction · 159% adiponectin increase",
            "dot_color": "#3b82f6"
          }
        }
      ]
    }
  ]
}
{% endschema %}